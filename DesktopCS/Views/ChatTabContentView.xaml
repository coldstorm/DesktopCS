<UserControl x:Class="DesktopCS.Views.ChatTabContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:behaviors="clr-namespace:DesktopCS.Behaviors"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
             
    <ScrollViewer Name="ChatScrollView">
        <RichTextBox Name="ChatRichTextBox" IsReadOnly="True" IsTabStop="False" IsDocumentEnabled="True" />
        <i:Interaction.Behaviors>
            <behaviors:AutoScrollBehavior />
        </i:Interaction.Behaviors>
    </ScrollViewer>
</UserControl>
